#!/bin/sh
branch="$(git rev-parse --abbrev-ref HEAD 2>/dev/null)"
if [ "$branch" = "main" ] || [ "$branch" = "master" ]; then
  echo "❌ Refusing to commit on '$branch'. Create a new branch."
  exit 1
fi
exit 0
